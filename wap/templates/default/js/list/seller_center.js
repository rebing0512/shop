$(function() {    $("#phone").click(function() {        if (!$(this).parent().hasClass("ok")) {            return false        }        var data = {}        if ($("#store_phone").length>0){            data.store_phone = $.trim($("#store_phone").val());        }        if ($("#store_description").length>0){            data.store_description = $.trim($("#store_description").val());        }            $.ajax({                type: "post",                url: ApiUrl + "/index.php?con=seller_stat&fun=update_phone",                data: data,                dataType: "json",                success: function(e) {                    if (e.code == 200) {                        layer.open({                            content: '设置成功',                            time:1.5                        });                        setTimeout(function () {                            window.MBC.back();                            return false;                        }, 2e3)                    } else {                        layer.open({                            content: e.datas.error,                            time:1.5                        });                    }                }            })    })          $.ajax({            type: "post",            url: ApiUrl + "/index.php?con=seller_center&fun=statistics",            dataType: "json",            success: function(a) {                $('.sale_goods').text(a.online);                $('.ck_goods').text(a.offline);                $('.wg_goods').text(a.lockup);                if(a.payment > 0){                    $('.state_new').show();                }                 if(a.delivery > 0){                    $('.state_pay').show();                }                if(a.send > 0){                    $('.state_send').show();                }                if(a.evalcount >0 ){                    $('.state_success').show();                }                if(a.cancel > 0){                     $('.state_cancel').show();                }                if(a.refund_lock >0 ){                    $('.order_refund').show();                }                if(a.refund > 0){                     $('.order_refund1').show();                }                 if(a.return_lock >0 ){                    $('.order_return').show();                }                if(a.return > 0){                     $('.order_return1').show();                }            }        })  });